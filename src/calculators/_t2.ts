import type { CalculatorDefinition } from "./types"; import { formatNumber } from "@/lib/utils"; export const test: CalculatorDefinition = { slug: "test", title: "Test", description: "Test calc", category: "Everyday", categorySlug: "everyday", icon: "~", keywords: ["test"], variants: [{ id: "v1", name: "V1", fields: [{ name: "x", label: "X", type: "number", placeholder: "1" }], calculate: (i) => { const x = (i.x as number) || 0; if (!x) return null; return { primary: { label: "Result", value: formatNumber(x) }, details: [{ label: "D", value: formatNumber(x * 2) }] }; } }], relatedSlugs: [], faq: [{ question: "Q?", answer: "A." }], formula: "x" };